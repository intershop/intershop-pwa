plugins {
  id "com.moowork.node" version "1.1.1"
}

node {
  version = '6.10.3'
  npmVersion = '3.10.10'
  download = false
  // distBaseUrl = Custom artifactory location here for node/npm.
}

task integrationTest(dependsOn: npmInstall)

task npmStart(type: NpmTask) {
    args = ['start']
}
