import { Routes } from '@angular/router';
import { ComparePageContainerComponent } from './compare-page.container';

export const comparePageRoutes: Routes = [
  { path: '', component: ComparePageContainerComponent }
];
