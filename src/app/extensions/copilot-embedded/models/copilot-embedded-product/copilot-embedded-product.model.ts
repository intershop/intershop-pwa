/**
 * A product recommended by the Copilot Embedded, extracted from the `icmSearch` tool output.
 */
export interface CopilotEmbeddedProduct {
  sku: string;
  title?: string;
  price?: number;
  currency?: string;
  imageUrl?: string;
  shortDescription?: string;
  manufacturer?: string;
}
