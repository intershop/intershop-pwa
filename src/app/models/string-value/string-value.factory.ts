import { FactoryHelper } from '../factory-helper';
import { StringValue } from './string-value.model';
import { Attribute } from '../attribute/attribute.model';

export class StringValueFactory {
  // static fromData(data: Attribute): StringValue {
  //   const stringValue: StringValue = new StringValue();
  //   FactoryHelper.primitiveMapping<Attribute, StringValue>(data, stringValue);
  //   return stringValue;
  // }
}
