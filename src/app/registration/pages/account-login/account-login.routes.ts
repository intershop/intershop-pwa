import { Routes } from '@angular/router';
import { AccountLoginComponent } from './account-login.component';

export const accountLoginRoutes: Routes = [
  { path: '', component: AccountLoginComponent }
];
