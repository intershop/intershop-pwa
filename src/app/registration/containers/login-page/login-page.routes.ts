import { Routes } from '@angular/router';
import { LoginPageContainerComponent } from './login-page.container';

export const loginPageRoutes: Routes = [{ path: '', component: LoginPageContainerComponent }];
