# LOGLEVEL=warn (default): Set log level threshold to warn (log warn and error only)
# Combined with LOG_ALL, this determines final logging behavior
# 2xx/3xx are considered "info" level, 4xx are "warn", 5xx are "error"
map $status $loglevel_loggable {
    ~^[23]  0;
    default 1;
}
